# The application resides in a directory ~/PlasSim/
# The following packages are required:
library(shiny) # first time? install.packages("shiny")
library(deSolve)
library(RColorBrewer)
# and now launch the shiny app:
runApp("PlasSim_v1.0")
